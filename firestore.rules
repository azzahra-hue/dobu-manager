rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{collection}/{docId} {
      allow read, update, delete: if resource.data.workspaceId == 'admin_bake2026';
      allow create: if request.resource.data.workspaceId == 'admin_bake2026';
    }
  }
}
